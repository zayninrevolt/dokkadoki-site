const test=require('node:test'),assert=require('node:assert/strict'),fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..');const read=f=>fs.readFileSync(path.join(root,f),'utf8');
test('returns policy covers current eBay sales',()=>{const s=read('content/returns.md');assert.ok(s.includes('eBay purchases'));assert.ok(!s.includes('We do not currently sell online'));assert.ok(!s.includes('covers purchases made at the café.\ncovers'));});
test('privacy documents newsletter processor and separate delivery logs',()=>{const s=read('content/privacy.md');assert.ok(s.includes('newsletter delivery'));assert.ok(s.includes('delivery logs'));});
test('MCM links use current organiser routes',()=>{const s=read('content/events/mcm-london-comiccon-2026/index.md');for(const p of ['help/travel-info.html','show-info/accessibility.html','things-to-do/cosplay-central.html'])assert.ok(s.includes(p),p);});
test('deployment enforces API and website tests',()=>{const s=read('.github/workflows/hugo.yaml');for(const p of ['npm ci','npm test','node --test scripts/test-*.cjs','python3 scripts/test-seo-templates.py','python3 scripts/test-ebay-copy.py'])assert.ok(s.includes(p),p);});
