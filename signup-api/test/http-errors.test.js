const test=require('node:test'),assert=require('node:assert/strict'),{spawnSync}=require('node:child_process'),path=require('node:path');
for(const [fault,status] of [['database',500],['url',400]])test(`HTTP ${fault} failures return a controlled response and preserve health`,()=>{const r=spawnSync(process.execPath,[path.join(__dirname,'http-fixture.cjs'),fault],{encoding:'utf8',timeout:5000,env:{PATH:process.env.PATH}});assert.equal(r.status,0,r.stderr);const d=JSON.parse(r.stdout);assert.equal(d.result.status,status);assert.equal(d.result.body.ok,false);assert.equal(d.health.status,200);assert.match(d.health.body.build,/^[a-f0-9]{64}$/);});
